# CRITICAL: MongoDB Connection
# The backend container MUST use the container name, not localhost
# Your MongoDB container is named 'mongo' (from docker ps output)
MONGODB_URI=mongodb://mongo:27017/meeting_app

# If your MongoDB has authentication, use this format instead:
# MONGODB_URI=mongodb://username:password@mongo:27017/meeting_app?authSource=admin

# JWT Secret (required for authentication)
JWT_SECRET=your_random_secret_key_change_this

# Optional: Google Sheets (only needed for data migration)
# GOOGLE_SHEETS_SOURCE_SPREADSHEET_ID=
# GOOGLE_SHEETS_TARGET_SPREADSHEET_ID=
# GOOGLE_SHEETS_CLIENT_EMAIL=
# GOOGLE_SHEETS_PRIVATE_KEY=
